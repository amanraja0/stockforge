const PageHeading = ({ children }) => {
  return (
    <h1 className="text-2xl md:text-[28px] font-semibold tracking-tight leading-tight mb-3">
      {children}
    </h1>
  );
};

export default PageHeading;
